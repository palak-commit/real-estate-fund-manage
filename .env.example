# Database (MySQL)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=real_estate_money
# Optional: per-instance pool size (serverless-friendly default is 5)
# DB_POOL_LIMIT=5

# Admin login (single static admin)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123321
# Secret used to sign the session cookie — use a long random string in production
AUTH_SECRET=change-this-to-a-long-random-string-in-production
